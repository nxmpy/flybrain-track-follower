name: tests
on: [push, pull_request]
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml
      - run: pip install -e ".[dev]"
      - run: ruff check .
      - run: pytest -q
      - run: python -m flybrain_robot.main --backend mock --synthetic --dry-run --steps 20
      - run: python -m flybrain_robot.main --backend optomotor-track --synthetic-track --output track --steps 60
      - run: python scripts/open_loop_test.py --steps 150
